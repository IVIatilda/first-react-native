import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  input: {
    height: 40,
    marginBottom: 20,
    borderWidth: 1,
    borderColor: 'tomato',
    color: 'tomato',
    padding: 10,
  },
});

export default styles;
