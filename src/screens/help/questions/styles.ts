import {StyleSheet} from 'react-native';

const styles = StyleSheet.create({
  questionItem: {
    marginBottom: 15,
  },
  question: {
    fontWeight: 'bold',
    fontSize: 15,
    marginBottom: 5,
  },
  answer: {
    fontSize: 14,
  },
});

export default styles;
