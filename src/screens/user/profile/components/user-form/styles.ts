import {StyleSheet} from 'react-native';

const styles = StyleSheet.create({
  inputBox: {
    marginBottom: 20,
  },
  input: {
    height: 40,
    borderWidth: 1,
    borderColor: 'silver',
    color: 'black',
    padding: 10,
  },
  error: {
    fontSize: 12,
    color: 'red',
    marginTop: 5,
  },
});

export default styles;
